apiVersion: v1
kind: Service
metadata:
  name: hotrod
  namespace: observability
  labels:
    app: hotrod
spec:
  ports:
    - port: 8080
      name: hotrod-http
  selector:
    app: hotrod
    tier: frontend
  type: LoadBalancer